# Stub indicator removed — the AAII sentiment spread was seeded as a
# placeholder with status: unknown and no verifiable source data. Delete
# this file from the catalogue when you can reach the repo from outside
# the sandbox; until then, compile.py treats it as invalid and skips it.
disabled: true
