# Stub indicator removed — the high-yield OAS was seeded as a placeholder
# with status: unknown and no verifiable live data feed. Delete this file
# from the catalogue when you can reach the repo from outside the sandbox;
# until then, compile.py treats it as invalid and skips it.
disabled: true
